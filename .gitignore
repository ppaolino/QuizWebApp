# Default Laravel ignores
/.phpunit.cache
/storage/*.key
/storage/pail
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Dependencies (reinstalled on server)
/node_modules
/vendor

# Development-only files (not needed in production)
/public/hot
/public/storage

# Keep these in repository (uncomment if already ignored)
/public/build/
